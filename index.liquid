---
layout: layout.liquid
title: Home
---

<div class="container">
    <h1>JSONPlaceholder API Data</h1>

    <!-- Alpine.js Data Fetching -->
    <div x-data="{
        posts: [], 
        users: [],
        async fetchData() {
            const postsResponse = await fetch('https://jsonplaceholder.typicode.com/posts');
            this.posts = await postsResponse.json();

            const usersResponse = await fetch('https://jsonplaceholder.typicode.com/users');
            this.users = await usersResponse.json();
        }
    }" x-init="fetchData()">

        <!-- Posts Section -->
        <div>
            <h2>Posts</h2>
            <ul>
                <template x-for="post in posts.slice(0, 5)" :key="post.id">
                    <li class="card" x-text="post.title"></li>
                </template>
            </ul>
        </div>

        <!-- Users Section -->
        <div>
            <h2>Users</h2>
            <ul>
                <template x-for="user in users.slice(0, 5)" :key="user.id">
                    <li class="card" x-text="user.name"></li>
                </template>
            </ul>
        </div>

    </div>
</div>
